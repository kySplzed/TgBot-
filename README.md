# Telegram Sales Bot

Полнофункциональный Telegram-бот для воронки продаж с интеграцией платежной системы YooKassa.

## 🚀 Возможности

- **Интерактивное меню** с inline кнопками
- **Информация о продукте** с детальным описанием
- **Цены и тарифы** с возможностью выбора
- **Система оплаты** через YooKassa
- **Управление подписками** с автоматическим продлением
- **Webhook обработка** платежей
- **Модульная архитектура** для легкого расширения

## 📦 Структура проекта

```
TgBot/
├── run_railway.py       # Entry point для Railway (бот + веб-сервер)
├── main.py              # Главный файл бота
├── webhook.py           # Webhook сервер для платежей
├── config.py            # Конфигурация и тарифы
├── messages.py          # Текстовые сообщения
├── keyboards.py         # Клавиатуры
├── handlers.py          # Обработчики команд
├── payments.py          # Логика оплаты
├── subscription.py      # Управление подписками
├── railway.json         # Конфигурация Railway
├── requirements.txt     # Зависимости
├── .env.example         # Пример переменных окружения
└── README.md           # Этот файл
```

## ⚙️ Настройка

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка YooKassa

1. **Зарегистрируйтесь** на [yookassa.ru/developers](https://yookassa.ru/developers)
2. **Создайте тестовый магазин**
3. **Получите API ключи**:
   - `shop_id` (идентификатор магазина)
   - `secret_key` (секретный ключ)
4. **Обновите config.py**:

```python
YOOKASSA_SHOP_ID = 'ваш_реальный_shop_id'
YOOKASSA_SECRET_KEY = 'ваш_реальный_secret_key'
```

### 3. Настройка webhook

1. **Запустите webhook сервер**:

```bash
python webhook.py
```

2. **Настройте URL webhook в YooKassa**:
   - В личном кабинете YooKassa перейдите в настройки магазина
   - Укажите URL: `https://ваш-домен.com/yookassa/webhook`
   - Выберите события: `payment.succeeded`

### 4. Настройка Telegram бота

1. **Создайте бота** через [@BotFather](https://t.me/botfather)
2. **Получите API токен**
3. **Обновите config.py**:

```python
API_TOKEN = 'ваш_токен_бота'
```

## 🚀 Запуск

### Быстрый запуск (рекомендуется)

1. **Скопируйте переменные окружения**:
```bash
cp .env.example .env
# Отредактируйте .env файл с реальными значениями
```

2. **Запустите приложение**:
```bash
./run.sh
```

### Ручной запуск

#### Локальное тестирование (без реальных платежей)

1. **Создайте виртуальное окружение**:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows
```

2. **Установите зависимости**:
```bash
pip install -r requirements.txt
```

3. **Запустите бота**:
```bash
python main.py
```

4. **Тестируйте интерфейс** в Telegram

5. **Имитируйте оплату** командой:
```bash
/testpay basic    # Для базового тарифа
/testpay premium  # Для премиум тарифа
/testpay vip      # Для VIP тарифа
```

#### Полноценный запуск с платежами

1. **Запустите webhook сервер**:
```bash
python webhook.py
```

2. **Запустите бота** в отдельном терминале:
```bash
python main.py
```

3. **Настройте webhook URL** в YooKassa на публичный адрес сервера

### Запуск через Docker

1. **Соберите и запустите контейнер**:
```bash
docker-compose up --build
```

### Тестирование webhook локально

Если webhook сервер запущен на `localhost:5000`, вы можете протестировать его через браузер:

```
http://localhost:5000/test-payment/ВАШ_TELEGRAM_ID/basic
```

Где:
- `ВАШ_TELEGRAM_ID` - ваш ID в Telegram (узнайте через `/status`)
- `basic` - план подписки (basic/premium/vip)

## 🎯 Использование

1. **Запустите бота** командой `/start`
2. **Ознакомьтесь с продуктом** - нажмите "📦 Подробная информация о продукте"
3. **Посмотрите цены** - нажмите "💰 Посмотреть цены и тарифы"
4. **Выберите тариф** и нажмите кнопку оплаты
5. **Оплатите** через YooKassa
6. **Получите подписку** - статус обновится автоматически через webhook

## 🔧 Команды бота

- `/start` - Запуск бота и очистка чата
- `/product` - Информация о продукте
- `/pricing` - Цены и тарифы
- `/subscribe` - Выбор тарифов
- `/status` - Статус подписки

## 📊 Тарифы

- **🟢 Базовый** - 999₽/месяц
- **🟡 Премиум** - 1999₽/месяц
- **🟠 VIP** - 3999₽/месяц

## 🔒 Безопасность

- Используйте HTTPS для webhook
- Храните API ключи в переменных окружения
- Регулярно обновляйте зависимости

## 🐛 Отладка

### Проблемы с webhook:

1. Проверьте, что сервер доступен: `curl http://localhost:5000/health`
2. Проверьте логи webhook сервера
3. Убедитесь, что URL webhook правильный

### Проблемы с ботом:

1. Проверьте токен бота
2. Проверьте логи бота в терминале
3. Убедитесь, что бот имеет права администратора (если нужно)

## 📈 Развертывание на Railway

### 🚂 Быстрое развертывание на Railway

1. **Создайте аккаунт** на [railway.app](https://railway.app)

2. **Подготовьте проект**:
   - Проект уже настроен для Railway с файлом `railway.json`
   - Все необходимые файлы очищены от лишнего

3. **Загрузите код на GitHub**:
```bash
# Создайте новый репозиторий на GitHub
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git push -u origin main
```

4. **Подключите Railway к GitHub**:
   - В Railway dashboard нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Подключите ваш репозиторий

5. **Настройте переменные окружения** в Railway:
   - Перейдите в "Variables" в настройках проекта
   - Добавьте переменные из `.env.example`:
     ```
     TELEGRAM_BOT_TOKEN=ваш_токен_бота
     YOOKASSA_SHOP_ID=ваш_shop_id
     YOOKASSA_SECRET_KEY=ваш_secret_key
     ENVIRONMENT=production
     DEBUG=false
     WEBHOOK_HOST=ваш_railway_url
     WEBHOOK_PORT=5000
     ```

6. **Railway автоматически развернет проект**:
   - Проект соберется с помощью Nixpacks
   - Приложение запустится на `python run_railway.py`
   - Будет доступен health check на `/health`

7. **Настройте webhook в YooKassa**:
   - В YooKassa укажите URL: `https://YOUR_RAILWAY_URL.up.railway.app/yookassa/webhook`
   - Выберите событие: `payment.succeeded`

### 🎯 Переменные окружения для Railway

Обязательные переменные:

```bash
# Telegram Bot
TELEGRAM_BOT_TOKEN=ВАШ_ТОКЕН_БОТА

# YooKassa Payment
YOOKASSA_SHOP_ID=ВАШ_YOOKASSA_SHOP_ID
YOOKASSA_SECRET_KEY=ВАШ_YOOKASSA_SECRET_KEY

# Application Settings
ENVIRONMENT=production
DEBUG=false

# Webhook Settings
WEBHOOK_HOST=https://YOUR_PROJECT.up.railway.app
WEBHOOK_PORT=5000
```

### 🔍 Мониторинг и отладка

1. **Логи Railway**: В dashboard Railway → "Logs"
2. **Health check**: `https://YOUR_PROJECT.up.railway.app/health`
3. **Тестирование платежей**: `https://YOUR_PROJECT.up.railway.app/test-payment/USER_ID/PLAN`

### 💡 Особенности Railway

- **Бесплатный тариф**: 512MB RAM, 1GB storage
- **Автоматическое масштабирование**
- **Встроенная база данных** (PostgreSQL/MySQL)
- **HTTPS по умолчанию**
- **Глобальная CDN**

### 🗄️ Добавление базы данных (опционально)

Если нужно заменить JSON на базу данных:

1. **Добавьте базу данных** в Railway dashboard
2. **Получите DATABASE_URL**
3. **Добавьте переменные окружения**:
   ```
   DATABASE_URL=postgresql://...
   DB_TYPE=postgresql
   ```

### Рекомендации для продакшена:

1. **Переменные окружения**: Все секреты хранятся в Railway Variables
2. **Мониторинг**: Используйте Railway logs и health checks
3. **Резервное копирование**: Railway автоматически бэкапит базы данных
4. **Безопасность**: HTTPS включен по умолчанию

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы:

- 📧 Email: support@yourcompany.com
- 💬 Telegram: @support_bot
- 📱 Телефон: +7 (999) 123-45-67

## 📄 Лицензия

MIT License - свободно используйте и модифицируйте код.
