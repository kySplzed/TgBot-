# Telegram Sales Bot

Полнофункциональный Telegram-бот для воронки продаж с интеграцией платежной системы YooKassa. Готов к деплою на render.com!

## 🚀 Возможности

- **Интерактивное меню** с inline кнопками
- **Информация о продукте** с детальным описанием
- **Цены и тарифы** с возможностью выбора
- **Система оплаты** через YooKassa с webhook
- **Управление подписками** с автоматическим продлением
- **Сбор данных пользователей** в базу данных
- **Обработка успешных и неудачных платежей**
- **Модульная архитектура** для легкого расширения
- **Логирование** всех операций

## 📦 Структура проекта

```
bot_project/
├── main.py                      # Точка входа в бота
├── config.py                    # Конфигурационные данные
├── Procfile                     # Инструкция для Render
├── requirements.txt             # Список зависимостей
├── README.md                    # Описание проекта
├── handlers/                    # Обработчики команд
│   ├── __init__.py
│   ├── start.py                 # Команды start и status
│   ├── product_info.py          # Информация о продукте
│   ├── subscription_flow.py     # Подписки и платежи
│   ├── payment_processing.py    # Обработка платежей
│   └── admin.py                 # Административные функции
├── services/                    # Бизнес-логика
│   ├── __init__.py
│   ├── product_service.py       # Логика продуктов
│   ├── subscription_service.py  # Управление подписками
│   ├── payment_service.py       # Обработка платежей
│   ├── user_service.py          # Работа с пользователями
│   └── messages.py              # Текстовые сообщения
├── db/                          # Работа с базой данных
│   ├── __init__.py
│   ├── models.py                # Модели данных
│   └── database.py              # Функции БД
├── keyboards/                   # Клавиатуры бота
│   ├── __init__.py
│   ├── inline_keyboards.py      # Inline клавиатуры
│   └── reply_keyboards.py       # Reply клавиатуры
├── utils/                       # Вспомогательные утилиты
│   ├── __init__.py
│   ├── state_manager.py         # Менеджер состояний
│   ├── logger.py                # Логирование
│   └── decorators.py            # Декораторы
└── assets/                      # Статические файлы
    └── __init__.py
```

## ⚙️ Настройка

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка YooKassa

1. **Зарегистрируйтесь** на [yookassa.ru/developers](https://yookassa.ru/developers)
2. **Создайте тестовый магазин**
3. **Получите API ключи**:
   - `shop_id` (идентификатор магазина)
   - `secret_key` (секретный ключ)
4. **Обновите config.py**:

```python
YOOKASSA_SHOP_ID = 'ваш_реальный_shop_id'
YOOKASSA_SECRET_KEY = 'ваш_реальный_secret_key'
```

### 3. Настройка webhook

1. **Запустите webhook сервер**:

```bash
python webhook.py
```

2. **Настройте URL webhook в YooKassa**:
   - В личном кабинете YooKassa перейдите в настройки магазина
   - Укажите URL: `https://ваш-домен.com/yookassa/webhook`
   - Выберите события: `payment.succeeded`

### 4. Настройка Telegram бота

1. **Создайте бота** через [@BotFather](https://t.me/botfather)
2. **Получите API токен**
3. **Обновите config.py**:

```python
API_TOKEN = 'ваш_токен_бота'
```

## 🚀 Запуск

### Быстрый запуск (рекомендуется)

1. **Скопируйте переменные окружения**:
```bash
cp .env.example .env
# Отредактируйте .env файл с реальными значениями
```

2. **Запустите приложение**:
```bash
./run.sh
```

### Ручной запуск

#### Локальное тестирование (без реальных платежей)

1. **Создайте виртуальное окружение**:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows
```

2. **Установите зависимости**:
```bash
pip install -r requirements.txt
```

3. **Запустите бота**:
```bash
python main.py
```

4. **Тестируйте интерфейс** в Telegram

5. **Имитируйте оплату** командой:
```bash
/testpay basic    # Для базового тарифа
/testpay premium  # Для премиум тарифа
/testpay vip      # Для VIP тарифа
```

#### Полноценный запуск с платежами

1. **Запустите webhook сервер**:
```bash
python webhook.py
```

2. **Запустите бота** в отдельном терминале:
```bash
python main.py
```

3. **Настройте webhook URL** в YooKassa на публичный адрес сервера

### Запуск через Docker

1. **Соберите и запустите контейнер**:
```bash
docker-compose up --build
```

### Тестирование webhook локально

Если webhook сервер запущен на `localhost:5000`, вы можете протестировать его через браузер:

```
http://localhost:5000/test-payment/ВАШ_TELEGRAM_ID/basic
```

Где:
- `ВАШ_TELEGRAM_ID` - ваш ID в Telegram (узнайте через `/status`)
- `basic` - план подписки (basic/premium/vip)

## 🎯 Использование

1. **Запустите бота** командой `/start`
2. **Ознакомьтесь с продуктом** - нажмите "📦 Подробная информация о продукте"
3. **Посмотрите цены** - нажмите "💰 Посмотреть цены и тарифы"
4. **Выберите тариф** и нажмите кнопку оплаты
5. **Оплатите** через YooKassa
6. **Получите подписку** - статус обновится автоматически через webhook

## 🔧 Команды бота

- `/start` - Запуск бота и очистка чата
- `/product` - Информация о продукте
- `/pricing` - Цены и тарифы
- `/subscribe` - Выбор тарифов
- `/status` - Статус подписки

## 📊 Тарифы

- **🟢 Базовый** - 999₽/месяц
- **🟡 Премиум** - 1999₽/месяц
- **🟠 VIP** - 3999₽/месяц

## 🔒 Безопасность

- Используйте HTTPS для webhook
- Храните API ключи в переменных окружения
- Регулярно обновляйте зависимости

## 🐛 Отладка

### Проблемы с webhook:

1. Проверьте, что сервер доступен: `curl http://localhost:5000/health`
2. Проверьте логи webhook сервера
3. Убедитесь, что URL webhook правильный

### Проблемы с ботом:

1. Проверьте токен бота
2. Проверьте логи бота в терминале
3. Убедитесь, что бот имеет права администратора (если нужно)



## � Развертывание на Render.com

### ⚡ Быстрое развертывание на Render

1. **Создайте аккаунт** на [render.com](https://render.com)

2. **Подготовьте проект**:
   - Проект уже настроен для Render с файлом `Procfile`
   - Структура очищена для production

3. **Загрузите код на GitHub**:
```bash
# Создайте новый репозиторий на GitHub
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git push -u origin main
```

4. **Подключите Render к GitHub**:
   - В Render dashboard нажмите "New +"
   - Выберите "Web Service"
   - Подключите ваш GitHub репозиторий

5. **Настройте сервис**:
   - **Name**: telegram-sales-bot (или любое другое)
   - **Environment**: Python 3
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python main.py`

6. **Настройте переменные окружения**:
   ```
   TELEGRAM_BOT_TOKEN=ваш_токен_бота
   YOOKASSA_SHOP_ID=ваш_yookassa_shop_id
   YOOKASSA_SECRET_KEY=ваш_yookassa_secret_key
   ENVIRONMENT=production
   DEBUG=false
   WEBHOOK_HOST=https://your-app-name.onrender.com
   WEBHOOK_PORT=10000
   ```

7. **Создайте сервис** - Render автоматически развернет приложение

8. **Настройте webhook в YooKassa**:
   - URL: `https://YOUR_APP_NAME.onrender.com/yookassa/webhook`
   - События: `payment.succeeded`, `payment.failed`

### 🎯 Переменные окружения для Render

Обязательные переменные:

```bash
# Telegram Bot
TELEGRAM_BOT_TOKEN=ВАШ_ТОКЕН_БОТА

# YooKassa Payment
YOOKASSA_SHOP_ID=ВАШ_YOOKASSA_SHOP_ID
YOOKASSA_SECRET_KEY=ВАШ_YOOKASSA_SECRET_KEY

# Application Settings
ENVIRONMENT=production
DEBUG=false

# Webhook Settings (ВАЖНО: порт 10000 для Render)
WEBHOOK_HOST=https://YOUR_APP_NAME.onrender.com
WEBHOOK_PORT=10000
```

### 🔍 Мониторинг на Render

1. **Логи**: Dashboard → Service → Logs
2. **Health check**: `https://YOUR_APP_NAME.onrender.com/health`
3. **Metrics**: Dashboard → Service → Metrics

### 💡 Особенности Render

- **Бесплатный тариф**: 750 часов/месяц, потом $7/месяц
- **Автоматическое масштабирование**
- **PostgreSQL база данных** (платная)
- **HTTPS по умолчанию**
- **Глобальная CDN**

### 🗄️ База данных на Render (опционально)

Для замены JSON файлов на PostgreSQL:

1. **Создайте PostgreSQL** в Render dashboard
2. **Получите DATABASE_URL**
3. **Добавьте переменные**:
   ```bash
   DATABASE_URL=postgresql://...
   DB_TYPE=postgresql
   ```

### 📋 Рекомендации для Render

1. **Переменные окружения**: Все секреты в Environment
2. **Мониторинг**: Используйте Render logs и health checks
3. **Резервное копирование**: Настраивается в настройках БД
4. **Безопасность**: HTTPS включен автоматически
5. **Производительность**: Используйте paid план для 24/7 работы

### 🚨 Важные замечания

- **Порт для webhook**: В Render используйте порт `10000`
- **Environment**: Обязательно установите `ENVIRONMENT=production`
- **Health check**: Добавьте health endpoint для мониторинга
- **Logs**: Проверяйте логи на ошибки платежей и webhook

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы:

- 📧 Email: support@yourcompany.com
- 💬 Telegram: @support_bot
- 📱 Телефон: +7 (999) 123-45-67

## 📄 Лицензия

MIT License - свободно используйте и модифицируйте код.
