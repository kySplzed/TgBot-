# 🤖 Telegram Sales Bot

Простой Telegram бот для продажи подписок с оплатой через YooKassa. Работает на render.com!

## ✨ Что умеет бот

- 📦 Показывает информацию о продукте
- 💰 Показывает цены и тарифы
- 💳 Принимает платежи через YooKassa
- 📊 Управляет подписками пользователей
- 🔄 Обрабатывает webhook уведомления
- 💾 Сохраняет данные пользователей

## 🛠️ Быстрая настройка

### 1. Установите зависимости
```bash
pip install -r requirements.txt
```

### 2. Настройте YooKassa
1. Зайдите на [yookassa.ru](https://yookassa.ru)
2. Создайте аккаунт и магазин
3. Получите `shop_id` и `secret_key`
4. Откройте `config.py` и вставьте ключи:

```python
YOOKASSA_SHOP_ID = 'ваш_shop_id_здесь'
YOOKASSA_SECRET_KEY = 'ваш_secret_key_здесь'
```

### 3. Настройте Telegram бота
1. Напишите [@BotFather](https://t.me/botfather) в Telegram
2. Создайте нового бота командой `/newbot`
3. Скопируйте токен бота
4. Откройте `config.py` и вставьте токен:

```python
API_TOKEN = 'ваш_токен_бота_здесь'
```

## 🚀 Запуск

### Локальный запуск (для тестирования)

1. **Установите переменные окружения:**
```bash
# Windows PowerShell
$env:ENVIRONMENT='development'
$env:DEBUG='true'

# Linux/Mac
export ENVIRONMENT=development
export DEBUG=true
```

2. **Запустите бота:**
```bash
python main.py
```

3. **Тестируйте в Telegram:**
   - Напишите боту `/start`
   - Попробуйте команды `/product`, `/pricing`, `/status`
   - Для теста оплаты: `/testpay basic`

### Запуск с webhook (для реальных платежей)

1. **Запустите webhook сервер:**
```bash
python webhook.py
```

2. **В отдельном терминале запустите бота:**
```bash
python main.py
```

3. **Проверьте работу:**
   - Откройте `http://127.0.0.1:5000/` в браузере
   - Должен открыться интерфейс сервера

## 🌐 Развертывание на Render.com

### ⚠️ **Важно! Ограничения Free плана:**

Render Free план **НЕ ПОДХОДИТ** для production ботов:
- 🔴 **Засыпает через 15 минут** бездействия (бот становится недоступен)
- 🔴 **Нет постоянного хранилища** (данные теряются при перезапуске)
- 🔴 **Нет SSH доступа** (сложно отлаживать)
- 🔴 **Нет масштабирования** (бот может "зависнуть" при нагрузке)

**Рекомендация:** Используйте **платный план** ($7/мес) или **Railway** для бота.

### Шаг 1: Создание сервиса
1. Создайте аккаунт на [render.com](https://render.com)
2. Загрузите код на GitHub
3. Нажмите "New +" → "Web Service"
4. Подключите ваш GitHub репозиторий

### Шаг 2: Настройка сервиса
Настройте параметры:
- **Name**: telegram-bot (или любое другое)
- **Environment**: Python 3
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `python main.py`

### Шаг 3: Получение WEBHOOK_HOST
После создания сервиса Render сгенерирует URL:
```
https://your-app-name.onrender.com
```
**Запомните этот URL!** Он понадобится для следующих шагов.

### Шаг 4: Переменные окружения
Добавьте переменные в разделе "Environment":
```
TELEGRAM_BOT_TOKEN=ваш_токен_бота
YOOKASSA_SHOP_ID=ваш_shop_id
YOOKASSA_SECRET_KEY=ваш_secret_key
ENVIRONMENT=production
DEBUG=false
WEBHOOK_HOST=https://your-app-name.onrender.com
WEBHOOK_PORT=10000
```

### Шаг 5: Настройка YooKassa
1. В YooKassa личном кабинете → Настройки магазина → HTTP-уведомления
2. URL для уведомлений:
   ```
   https://your-app-name.onrender.com/yookassa/webhook
   ```
3. Выберите события: `payment.succeeded`

### 🔔 Как "будить" бота на Free плане

Free инстансы засыпают через 15 минут. Вот способы "будить" бота:

#### **Вариант 1: Health check**
Посещайте URL каждые 10-15 минут:
```
https://your-app-name.onrender.com/health
```

#### **Вариант 2: Uptime мониторы**
Используйте сервисы вроде:
- [UptimeRobot](https://uptimerobot.com) (бесплатно)
- [Healthchecks.io](https://healthchecks.io)
- Настройте мониторинг `/health` эндпоинта

#### **Вариант 3: Cron job**
Настройте автоматические запросы к `/health` каждые 10 минут.

### 🎯 **Результат:**
- ✅ Сервис развернут на Render
- ✅ Переменные окружения настроены
- ✅ YooKassa подключена
- ✅ Бот работает (если не спит)

## 📋 Команды бота

- `/start` - Начать работу с ботом
- `/product` - Информация о продукте
- `/pricing` - Посмотреть цены
- `/subscribe` - Выбрать подписку
- `/status` - Проверить статус подписки

## 📊 Тарифы

- **🟢 Базовый** - 999₽/месяц
- **🟡 Премиум** - 1999₽/месяц
- **🟠 VIP** - 3999₽/месяц

## 🔧 Техническая информация

### Структура проекта
```
├── main.py              # Главный файл бота
├── webhook.py           # Сервер для webhook
├── config.py            # Настройки
├── handlers/            # Обработчики команд
├── services/            # Бизнес-логика
├── db/                  # Работа с данными
├── keyboards/           # Кнопки бота
└── utils/               # Вспомогательные функции
```

### Webhook эндпоинты
- `GET /` - Главная страница сервера
- `GET /health` - Проверка работоспособности
- `POST /yookassa/webhook` - Обработка платежей
- `GET /test-payment/<id>/<plan>` - Тестовый платеж

## 🆘 Проблемы и решения

### Бот не отвечает
- Проверьте токен в `config.py`
- Убедитесь что бот запущен: `python main.py`

### Webhook не работает
- Проверьте что сервер запущен: `python webhook.py`
- Откройте `http://127.0.0.1:5000/health`
- Проверьте логи в терминале

### Оплата не проходит
- Проверьте ключи YooKassa в `config.py`
- Убедитесь что webhook URL правильный
- Проверьте логи платежей

## 📞 Поддержка

Если что-то не работает:
1. Проверьте логи в терминале
2. Убедитесь что все ключи указаны правильно
3. Попробуйте перезапустить сервисы
